spring:
  datasource:
    url: jdbc:postgresql://localhost:5000/club27db
    username: club27user
    password: 1234
    driver-class-name: org.postgresql.Driver
    initialization-mode: always

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create

users:
  listing:
    pageSize: 12